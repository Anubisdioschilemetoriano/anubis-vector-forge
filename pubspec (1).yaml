name: ultra_vectorizer
description: Motor de vectorización nativo Kotlin/C++ con Potrace para Flutter.
version: 1.0.0+Omega

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.ultra.vectorizer
        pluginClass: UltraVectorizerPlugin
